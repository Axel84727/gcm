## v0.1 – Core mínimo 

### Minimal core

1. Lists tracked files with changes (git status --porcelain)
2. Allows selecting files to commit
3. Internally executes:
    - git add <files>
    - git commit
4. Exits